spring:
  application:
    name: @project.artifactId@
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: redis

server:
  port: @server.port@


openweather:
  apiKey:
  url: "https://api.openweathermap.org/"

springdoc:
  api-docs:
    enabled: true
    path: /api-docs